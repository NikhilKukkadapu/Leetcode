class Solution {
    public boolean isPowerOfFour(int n) {
        if( n  <= 0) return false;
        if( n == 1) return true;
        double res = Math.log(n) / Math.log(4);
        return (n % 4 == 0) && isPowerOfFour(n / 4);
    }
}
