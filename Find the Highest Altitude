class Solution {
    public int largestAltitude(int[] gain) {
        int current = 0 , x = 0;
        for(int i = 0 ; i < gain.length ; i++){
            current += gain[i];
            int num = Math.max(x,current);
            x = num;
        }
        return x;
    }
}
